# ===========================================
# Sanity CMS Configuration
# ===========================================

# Get these from https://www.sanity.io/manage
# Create a new project or use an existing one

# Your Sanity project ID (found in sanity.io/manage)
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id

# Dataset name (usually 'production' or 'development')
NEXT_PUBLIC_SANITY_DATASET=production

# API version (use date format: YYYY-MM-DD)
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# Optional: Token for authenticated requests (for drafts/preview)
# Create this at sanity.io/manage -> API -> Tokens
SANITY_API_READ_TOKEN=your_read_token

# CORS (required for Studio to save/publish when embedded in your site):
# Add your Studio origin in https://manage.sanity.io → Project → API → CORS origins
# with "Allow credentials" enabled. Examples:
#   Local:    http://localhost:3000  (no trailing slash)
#   Deployed: https://your-domain.com
# Or run: npx sanity cors add http://localhost:3000 --credentials
#         npx sanity cors add https://your-domain.com --credentials

# ===========================================
# Shopify Storefront API Configuration
# ===========================================

# Your Shopify store domain (e.g., your-store.myshopify.com)
SHOPIFY_STORE_DOMAIN=your-store.myshopify.com

# Shopify Storefront Access Token
# Create this in Shopify Admin -> Settings -> Apps and sales channels -> Develop apps
# Enable Storefront API with these scopes:
# - unauthenticated_read_product_listings
# - unauthenticated_read_customers
# - unauthenticated_write_checkouts
# - unauthenticated_read_checkouts
SHOPIFY_STOREFRONT_ACCESS_TOKEN=your_storefront_access_token

# Public domain for client-side requests (same as SHOPIFY_STORE_DOMAIN but public)
NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN=your-store.myshopify.com

# ===========================================
# Google Calendar API (Service Account)
# ===========================================
# 1. Google Cloud Console: enable Calendar API
# 2. Create a Service Account, download JSON key
# 3. From the JSON: use "client_email" and "private_key"
# 4. Share your Google Calendar with the service account email
#    (edit calendar → Share with specific people → add that email, “Make changes to events”)

# Service account email (copy from JSON file's "client_email" field)
GOOGLE_SERVICE_ACCOUNT_EMAIL=vision@scandicommerce.iam.gserviceaccount.com

# Private key: copy the entire "private_key" value from the JSON file
# It should start with "-----BEGIN PRIVATE KEY-----" and end with "-----END PRIVATE KEY-----"
# In .env: use \n for newlines inside double quotes, or paste the whole key on one line
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...\n-----END PRIVATE KEY-----\n"

# Calendar to read/write (e.g. your@gmail.com)
GOOGLE_CALENDAR_ID=your@gmail.com

# ===========================================
# Contact form (Send us message → admin email)
# ===========================================
# Admin email that receives contact form submissions
CONTACT_ADMIN_EMAIL=admin@example.com

# Optional: "From" address and name in the email (defaults to SMTP_USER and name below)
CONTACT_FROM_EMAIL=noreply@yourdomain.com
CONTACT_FROM_NAME=Scandicommerce Contact Form

# SMTP (e.g. Gmail, SendGrid, Mailgun). Required for contact form to send email.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password

https://myaccount.google.com/apppasswords (password)
# ===========================================
# Site URL (for hreflang / canonical / sitemap)
# ===========================================
# Absolute origin with no trailing slash (e.g. https://scandicommerce.com)
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# ===========================================
# Existing Environment Variables
# ===========================================

# Add any other environment variables your project needs here
